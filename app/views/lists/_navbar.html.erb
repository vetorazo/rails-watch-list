<nav class="navbar navbar-expand-lg navbar-lewagon shadow-sm mb-4">
  <div class="container-fluid">
    <span class="navbar-brand mb-0 h1 display-6" style="font-weight: 600; letter-spacing: 1px;">
      <i class="fa fa-film me-2"></i>
      My Watch Lists
    </span>
    <div class="d-flex">
      <% if button %>
        <%= link_to "Create new list", new_list_path, class: "btn btn-primary rounded-pill px-4 py-2 fw-semibold shadow-sm" %>
      <% else %>
        <%= link_to "Back to lists", lists_path, class: "btn btn-outline-primary rounded-pill px-4 py-2 fw-semibold shadow-sm" %>
      <% end %>
    </div>
  </div>
</nav>

<div class="banner-bg"></div>